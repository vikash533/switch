#include<stdio.h>
void area(int,int);
void main()
{
    int l,b;
    printf("enter the length and breadth of rec");
    scanf("%d%d",&l,&b);

}
void area(int x,int y)
{
    int area;
    area=(x*y);
    printf("area of rec =%d",area);

}